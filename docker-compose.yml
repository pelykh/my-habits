services:
  habits:
    build: .
    ports:
      - "${PORT:-3001}:${PORT:-3001}"
    volumes:
      - ./data:/data
    environment:
      DB_PATH: /data/habits.db
      PORT: ${PORT:-3001}
    stop_grace_period: 10s
    restart: unless-stopped
